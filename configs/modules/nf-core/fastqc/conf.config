process {
    withName: 'FASTQC' {
        ext.args         = '--quiet'
        publishDir       = [
            [
                path: { "${params.out_dir}/reports/fastqc/${meta.id}" },
                mode: "copy",
                pattern: "*"
            ]
        ]
    }
}