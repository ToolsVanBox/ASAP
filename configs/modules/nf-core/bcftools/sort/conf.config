process {
    withName: 'BCFTOOLS_SORT' {
        ext.args         = ''
        ext.prefix       = { "${meta.id}.sorted" }
        publishDir       = [
            //specify to avoid publishing, overwritten otherwise
            enabled: false
        ]                     
    }
}