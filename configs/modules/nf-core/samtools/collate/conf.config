process {
    withName: 'SAMTOOLS_COLLATE' {
        ext.args = '--output-fmt bam'
        publishDir       = [
            //specify to avoid publishing, overwritten otherwise
            enabled: false
        ]         
    }
}