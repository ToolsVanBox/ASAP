process {
    withName: 'LILAC' {
        publishDir       = [
            [
                path: { "${params.out_dir}/hla_type_calling/${meta.id}/" },
                mode: "copy",
                pattern: "lilac"
            ]
        ]
    }
}